## What & why

<!-- Link the issue this closes, e.g. Closes #12 -->

## Test plan

- [ ] Feature PRs: includes a happy-path test
- [ ] Bug-fix PRs: includes a regression test that failed before this change and passes after it
- [ ] `uv run ruff check .` / `uv run ruff format --check .` / `uv run ty check` / `uv run pytest -m "not network"` all pass locally

## Out of scope / follow-ups

<!-- Anything deliberately deferred, and to which issue -->
