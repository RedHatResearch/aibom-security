[project]
name = "aibom-verifier"
version = "0.1.0"
description = "Passive, weight-level verification of LLM base_model provenance claims"
readme = "README.md"
authors = [
    { name = "Ali Al-Sorehi", email = "aalsoreh@redhat.com" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
