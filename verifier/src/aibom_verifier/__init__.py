"""Passive, weight-level verification of LLM ``base_model`` provenance claims.

This package is a library consumed by the ``aibom`` umbrella CLI (see the
sibling ``cli`` workspace member) — it is not meant to be installed or run on
its own.
"""

__version__ = "0.1.0"
